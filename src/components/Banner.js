import React from 'react';
//Banner component that renders a <header> tag with specified text and <p> tag with instructionsfor users.
function Banner() {
   return (
      <header>
         <h1>Orbit Report</h1>
         <p>Click on the buttons to see the satellites in that orbit type</p>
      </header>
   );
}

export default Banner;
